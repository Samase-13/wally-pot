// Selecciona la imagen por su ID
const imagen = document.getElementById('mi-imagen');

// Asigna un controlador de eventos para el clic en la imagen
imagen.addEventListener('click', () => {
  // Redirige al usuario a otro HTML
  window.location.href = './index.html';
});
